[pytest]
markers =
    timeout: per-test timeout in seconds (requires pytest-timeout plugin)
    smoke: fast sanity checks to catch gross failures early
timeout = 60
timeout_method = thread
timeout_func_only = true
